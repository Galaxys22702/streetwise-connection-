{
  "name": "streetwise-connection",
  "version": "0.4.0",
  "private": true,
  "description": "MVP control plane and storefront for affordable eSIM-based mobile data service.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "db:migrate": "node scripts/migrate.js",
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage",
    "verify": "npm run test:coverage && node scripts/vercel-config-check.js && node scripts/launch-readiness-check.js",
    "check:production": "node scripts/production-smoke-test.js"
  },
  "dependencies": {
    "pg": "^8.13.0",
    "stripe": "^18.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
